using UnityEngine;

public class SeedGameManager : MonoBehaviour
{

    [SerializeField] GameObject playerRef;
    [SerializeField] ScriptableObject dataObject;

    // Update is called once per frame
    void Update()
    {
        
    }
}
